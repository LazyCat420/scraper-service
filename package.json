{
  "name": "scraper-service",
  "version": "0.1.0",
  "description": "General-purpose scraping microservice — domain-agnostic",
  "main": "app/main.py",
  "scripts": {
    "dev": "uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8001"
  }
}

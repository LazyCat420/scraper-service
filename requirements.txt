# ============================================================
# scraper-service — Production Dependencies
# ============================================================
# Pins: all packages >= 14 days old to avoid supply-chain risk.

# Core: HTTP client, API framework, config
fastapi==0.115.12
uvicorn[standard]==0.34.2
httpx==0.28.1
pydantic==2.11.3
pydantic-settings==2.13.1
python-dotenv==1.2.2

# HTML parsing / text extraction
beautifulsoup4==4.14.3
lxml>=5.0.0
trafilatura>=1.6.0
feedparser==6.0.12
fake-useragent>=2.0.3
yfinance==0.2.40

# Cloudflare bypass
cloudscraper>=1.2.71

# YouTube
yt-dlp==2026.3.17
youtube-transcript-api==1.2.4

# Browser automation (Playwright — install chromium separately)
# playwright is installed via `pip install playwright && playwright install chromium --with-deps`

# crawl4ai (advanced scraping engine)
crawl4ai>=0.8.5

# Retry / resilience
tenacity>=8.5.0

# Logging
structlog>=24.4.0

# HTTP compat
urllib3==1.26.18
chardet==5.2.0

# DDG Search
ddgs==9.11.4

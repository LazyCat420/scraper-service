# ============================================================
# scraper-service — Production Dependencies
# ============================================================
# Pins track trading-service's proven lock for the overlapping packages
# (the scraper's real home). Slim, scraper-scoped — no numpy/pandas/scipy.
# NOTE: not pip-compiled here. Run `pip-compile` before a production build if
# transitive resolution surprises you; these are the direct deps app.scraper
# actually imports.

# Core: HTTP client, API framework, config
fastapi==0.115.12
uvicorn[standard]==0.34.2
httpx[brotli,http2,socks]==0.28.1
pydantic==2.11.3
pydantic-settings==2.13.1
python-dotenv==1.2.2

# HTML parsing / text extraction
beautifulsoup4==4.14.3
lxml>=5.0.0
trafilatura==2.1.0
feedparser==6.0.12
fake-useragent>=2.0.3
yfinance==0.2.40

# Cloudflare bypass
cloudscraper>=1.2.71

# YouTube
yt-dlp==2026.3.17
youtube-transcript-api==1.2.4

# Browser automation — Playwright (chromium installed separately in Dockerfile)
playwright==1.61.0
playwright-stealth==2.0.3

# crawl4ai (advanced scraping engine)
crawl4ai==0.9.2

# Retry / resilience
tenacity>=8.5.0

# Logging
structlog>=24.4.0

# HTTP compat
urllib3==1.26.18
chardet==5.2.0

# DDG Search
ddgs==9.11.4

# Twitter/X scraping
twscrape==0.19.1
